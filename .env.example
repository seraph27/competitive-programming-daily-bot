# Environment Variables for LeetCode Daily Discord Bot
# NOTE: config.toml is the recommended configuration method.
# These environment variables are provided for backward compatibility.
# If both config.toml and .env exist, environment variables will override config.toml values.

# Discord bot token
DISCORD_TOKEN=your_discord_bot_token_here

# Default post time (24-hour format, HH:MM)
POST_TIME=00:00 

# Default timezone (Example: UTC, Asia/Taipei, America/New_York)
TIMEZONE=UTC

# Google Gemini API Key (optional - for AI translation and inspiration features)
GOOGLE_GEMINI_API_KEY=your_google_gemini_api_key_here